/* Round 32 — restrained frosted header + luminous physical card system.
   The card language follows the user's portfolio boards: light faces, thin outlines,
   small-radius corners, precise edge highlights, and restrained physical depth. */

:root{
  --page-gutter:clamp(26px,3.15vw,58px);
  --side-w:clamp(255px,16.2vw,305px);
  --panel-top:clamp(132px,18vh,174px);
  --panel-bottom:clamp(44px,8vh,76px);
  --panel-gap:12px;
  --panel-radius:18px;
  --card-ink:#46515a;
  --card-ink-soft:#75818a;
  --luminous-face:rgba(239,244,247,.94);
  --luminous-face-2:rgba(221,229,234,.92);
  --luminous-edge:rgba(255,255,255,.84);
}

/* Secondary-page glass shaders are deliberately not used for these surfaces in Round 32.
   The visible body is a model-like luminous panel instead of another layer of glass. */
.liquid-panel-dom{
  -webkit-backdrop-filter:none!important;
  backdrop-filter:none!important;
}

.category-panel{
  left:var(--page-gutter)!important;
  top:var(--panel-top)!important;
  width:var(--side-w)!important;
  min-height:clamp(348px,52vh,520px);
  padding:30px 30px 28px!important;
  border-radius:var(--panel-radius)!important;
  overflow:hidden!important;
  color:rgba(250,252,253,.96)!important;
  background:
    radial-gradient(120% 90% at 12% 0%,rgba(255,255,255,.18),transparent 52%),
    linear-gradient(150deg,rgba(157,171,180,.78),rgba(116,130,140,.72) 58%,rgba(103,117,127,.74));
  border:1px solid rgba(255,255,255,.52)!important;
  box-shadow:
    0 18px 38px rgba(18,25,30,.15),
    0 2px 5px rgba(18,25,30,.08),
    inset 0 1px 0 rgba(255,255,255,.68),
    inset 1px 0 0 rgba(255,255,255,.20),
    inset 0 -1px 0 rgba(35,45,51,.12)!important;
}
.category-panel::before{
  content:"";
  position:absolute;
  inset:1px 1px auto;
  height:36%;
  border-radius:calc(var(--panel-radius) - 2px) calc(var(--panel-radius) - 2px) 40% 40%;
  pointer-events:none;
  background:linear-gradient(180deg,rgba(255,255,255,.12),rgba(255,255,255,0));
}
.category-panel>*{position:relative;z-index:1}

#number{
  display:block;
  margin-bottom:8px;
  font-family:var(--font-ui);
  font-size:12px!important;
  font-weight:520!important;
  letter-spacing:.10em!important;
  color:rgba(248,251,252,.76)!important;
}
#category-title{
  max-width:11ch;
  margin:4px 0 0!important;
  font-family:var(--font-display);
  font-size:clamp(36px,2.75vw,48px)!important;
  font-weight:400!important;
  line-height:.98!important;
  letter-spacing:-.025em!important;
  color:rgba(255,255,255,.98)!important;
  text-shadow:0 1px 14px rgba(31,41,47,.11);
}
.category-panel>.rule{
  width:34px!important;
  margin:22px 0 20px!important;
  background:rgba(255,255,255,.72)!important;
}
#filters{
  display:flex;
  flex-direction:column;
  align-items:stretch!important;
  gap:5px!important;
  font-family:var(--font-ui);
  font-size:16px!important;
  font-weight:450!important;
  letter-spacing:.002em!important;
}
#filters button{
  position:relative;
  width:100%;
  padding:9px 12px!important;
  border-radius:8px;
  text-align:left;
  color:rgba(248,251,252,.74)!important;
  opacity:1!important;
  translate:0 0!important;
  transition:color .22s ease,background .26s ease,transform .30s var(--ease-out)!important;
}
#filters button::before{
  content:"";
  position:absolute;
  left:0;top:7px;bottom:7px;
  width:3px;
  border-radius:3px;
  background:rgba(255,255,255,.94);
  opacity:0;
  transform:scaleY(.55);
  transition:opacity .24s ease,transform .30s var(--ease-out);
}
#filters button:hover,
#filters button.active{
  color:#fff!important;
  background:linear-gradient(90deg,rgba(255,255,255,.15),rgba(255,255,255,.035));
  transform:translateX(2px)!important;
}
#filters button:hover::before,
#filters button.active::before{opacity:1;transform:scaleY(1)}
#filters button::after{display:none!important}

.category-actions{
  position:absolute;
  left:30px;
  right:30px;
  bottom:26px;
  margin:0!important;
  gap:9px!important;
}
#enter,#back{
  font-family:var(--font-ui);
  color:rgba(250,252,253,.82)!important;
  font-size:13px!important;
  font-weight:500!important;
  letter-spacing:.02em!important;
  transition:color .22s ease,transform .30s var(--ease-out)!important;
}
#enter{font-size:15px!important;color:#fff!important}
#enter:hover,#back:hover{color:#fff!important;transform:translateX(4px)!important}

.index-content{
  left:calc(var(--page-gutter) + var(--side-w) + var(--panel-gap))!important;
  right:var(--page-gutter)!important;
  top:var(--panel-top)!important;
  bottom:var(--panel-bottom)!important;
  padding:0!important;
  border-radius:0!important;
  overflow:auto!important;
  color:var(--card-ink)!important;
  background:transparent!important;
  border:0!important;
  box-shadow:none!important;
  scrollbar-width:thin;
  scrollbar-color:rgba(255,255,255,.30) transparent;
}
.index-content::-webkit-scrollbar{width:5px;height:5px}
.index-content::-webkit-scrollbar-thumb{background:rgba(255,255,255,.28);border-radius:10px}

.gallery{
  display:grid!important;
  grid-template-columns:repeat(4,minmax(0,1fr))!important;
  gap:var(--panel-gap)!important;
  align-items:stretch;
  min-height:100%;
}
.gallery.research{grid-template-columns:repeat(2,minmax(0,1fr))!important}
.gallery button{
  position:relative;
  display:flex;
  flex-direction:column;
  min-width:0;
  align-self:stretch!important;
  padding:10px 10px 16px!important;
  overflow:hidden;
  text-align:left;
  color:var(--card-ink)!important;
  border-radius:var(--panel-radius)!important;
  background:
    radial-gradient(105% 72% at 20% 0%,rgba(255,255,255,.74),transparent 58%),
    linear-gradient(145deg,var(--luminous-face),var(--luminous-face-2));
  border:1px solid var(--luminous-edge)!important;
  box-shadow:
    0 15px 34px rgba(22,30,35,.13),
    0 3px 7px rgba(22,30,35,.08),
    inset 0 1px 0 rgba(255,255,255,.96),
    inset 1px 0 0 rgba(255,255,255,.36),
    inset 0 -1px 0 rgba(110,125,134,.10)!important;
  transform:translateY(0);
  transition:transform .38s var(--ease-out),box-shadow .38s ease,background .38s ease!important;
}
.gallery button::before{
  content:"";
  position:absolute;
  left:7px;right:7px;top:2px;height:1px;
  border-radius:999px;
  background:linear-gradient(90deg,transparent,rgba(255,255,255,.94),transparent);
  opacity:.72;
  pointer-events:none;
}
.gallery button:hover{
  transform:translateY(-4px)!important;
  box-shadow:
    0 20px 44px rgba(22,30,35,.16),
    0 4px 9px rgba(22,30,35,.08),
    inset 0 1px 0 rgba(255,255,255,.98),
    inset 1px 0 0 rgba(255,255,255,.42),
    inset 0 -1px 0 rgba(110,125,134,.10)!important;
}
.gallery img{
  width:100%;
  aspect-ratio:1/1.02!important;
  object-fit:cover;
  flex:none;
  margin:0 0 15px!important;
  border-radius:11px!important;
  border:1px solid rgba(255,255,255,.70);
  filter:grayscale(.08) contrast(1.015)!important;
  box-shadow:
    0 7px 16px rgba(34,43,49,.10),
    inset 0 1px 0 rgba(255,255,255,.50)!important;
  transition:filter .35s ease,transform .40s var(--ease-out)!important;
}
.gallery button:hover img{filter:grayscale(0) contrast(1.025)!important;transform:scale(1.006)!important}
.gallery button>span:not(.item-arrow){display:block;min-width:0}
.gallery .item-title{
  display:block;
  margin:0 0 5px;
  font-family:var(--font-display);
  font-size:clamp(19px,1.35vw,25px)!important;
  font-weight:400!important;
  line-height:1.02!important;
  letter-spacing:-.022em!important;
  color:#3f4a53!important;
}
.gallery .item-meta{
  display:block;
  margin-top:3px!important;
  font-family:var(--font-ui);
  font-size:12px!important;
  font-weight:450!important;
  line-height:1.28;
  letter-spacing:.006em!important;
  color:#77838c!important;
}
.item-arrow{
  position:absolute;
  right:16px;
  bottom:14px;
  font-family:var(--font-ui);
  font-size:20px;
  line-height:1;
  color:#64717a;
  transition:transform .3s var(--ease-out),color .2s ease;
}
.gallery button:hover .item-arrow{transform:translate(2px,-2px);color:#3f4a53}

/* Writing index adopts the same physical-panel language without changing its content layout. */
.writing-list{display:flex!important;flex-direction:column;gap:10px;height:auto!important}
.writing-row{
  position:relative;
  display:grid!important;
  grid-template-columns:17% 1fr 18% 22px!important;
  gap:3%!important;
  align-items:center;
  min-height:122px!important;
  padding:12px!important;
  border:1px solid var(--luminous-edge)!important;
  border-radius:16px;
  color:var(--card-ink)!important;
  background:linear-gradient(145deg,var(--luminous-face),var(--luminous-face-2));
  box-shadow:0 12px 28px rgba(22,30,35,.12),inset 0 1px 0 rgba(255,255,255,.92);
  transition:transform .34s var(--ease-out),box-shadow .34s ease!important;
}
.writing-row::before{display:none!important}
.writing-row:hover{transform:translateX(4px)!important;box-shadow:0 16px 34px rgba(22,30,35,.15),inset 0 1px 0 rgba(255,255,255,.95)}
.writing-row img{height:90px!important;border-radius:10px!important;filter:grayscale(.10)!important;box-shadow:0 6px 15px rgba(34,43,49,.10)!important}
.writing-row .item-title{font-family:var(--font-display);font-size:23px!important;color:#3f4a53!important}
.writing-row .item-description,.writing-row .item-meta{font-family:var(--font-ui);font-size:12px!important;color:#77838c!important}

body.preview .index-content{display:none!important}
body.index #scene{opacity:.34;transition:opacity .35s ease}
body.index .category-panel{width:var(--side-w)!important}
body.index #category-title{font-size:clamp(34px,2.55vw,46px)!important}
body.index #filters{font-size:16px!important;gap:5px!important;margin-top:0!important}
body.index #enter{display:none}

/* The home labels stay editorial and quiet. */
.object-label{transition:color .2s ease,opacity .2s ease,filter .3s ease}
.object-label em{font-family:var(--font-display);font-weight:400;letter-spacing:-.018em;transition:transform .42s var(--ease-out),letter-spacing .42s var(--ease-out)}
.object-label:hover em{transform:translateY(-3px);letter-spacing:.01em}
.object-label small{font-family:var(--font-ui);font-style:normal;font-size:10px;font-weight:500;letter-spacing:.08em}
.object-label:hover{filter:drop-shadow(0 7px 16px rgba(255,255,255,.12))}

.site-footer{font-family:var(--font-ui);font-size:9px;font-weight:450;letter-spacing:.045em;opacity:.86}

/* Modal remains glass; it is the only secondary framebuffer-glass surface in Round 32. */
#info{
  width:min(560px,88vw);
  padding:52px 50px 46px;
  color:var(--ui-white);
  border-radius:26px;
}
#info::backdrop{background:rgba(8,12,15,.10);-webkit-backdrop-filter:blur(3px);backdrop-filter:blur(3px)}
#info h2{font-family:var(--font-display);font-size:36px;font-weight:400;letter-spacing:-.026em;line-height:1;margin-bottom:24px}
#info p{max-width:36ch;font-family:var(--font-ui);font-size:14px;font-weight:430;line-height:1.5;letter-spacing:.005em;color:rgba(239,245,247,.78)}
#info .close{right:21px;top:14px;width:34px;height:34px;display:grid;place-items:center;font-family:var(--font-ui);font-size:25px;border-radius:50%;color:rgba(245,249,250,.68);transition:transform .34s var(--ease-out),color .24s ease}
#info .close:hover{transform:rotate(90deg);color:#fff}

@media(max-width:1180px){
  :root{--side-w:clamp(235px,22vw,285px);--page-gutter:24px}
  .gallery{grid-template-columns:repeat(2,minmax(0,1fr))!important}
}
@media(max-width:820px){
  :root{--panel-top:104px;--page-gutter:18px;--side-w:min(calc(100vw - 36px),420px)}
  .category-panel{min-height:280px;padding:24px!important}
  .category-actions{left:24px;right:24px;bottom:20px}
  .index-content{left:18px!important;right:18px!important;top:410px!important;bottom:18px!important}
  .gallery{grid-template-columns:repeat(2,minmax(0,1fr))!important}
  #category-title{font-size:34px!important}
  #info{padding:42px 30px 34px}
}
@media(max-width:590px){
  .gallery,.gallery.research{grid-template-columns:1fr!important}
  .writing-row{grid-template-columns:28% 1fr 20px!important}
  .writing-row .date{display:none}
}

/* Keep the secondary workspace luminous; do not sink it into the old charcoal index treatment. */
body.index{background:#aeb7bd!important}
body.index #scene{opacity:.82!important}

#info{
  background:transparent!important;
  border:0!important;
  box-shadow:none!important;
  -webkit-backdrop-filter:none!important;
  backdrop-filter:none!important;
}
